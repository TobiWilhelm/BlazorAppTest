---
apiVersion: v1
kind: Service
metadata:
  name: TestApp
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: TestApp
  labels:
    app: TestApp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: TestApp
  template:
    metadata:
      labels:
        app: TestApp
    spec:
      containers:
      - name: nginx
        # image: us-central1-docker.pkg.dev/GOOGLE_PROJECT/demo/nginx:latest
        image: europe-west3-docker.pkg.dev/PROJECT_ID/samples/my-blazor-image:latest"